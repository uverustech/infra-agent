name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      - name: Build binaries
        run: |
          GOOS=linux GOARCH=amd64 go build -ldflags="-s -w" -o gtw-agent-linux-amd64 ./cmd/gtw-agent
          GOOS=linux GOARCH=arm64 go build -ldflags="-s -w" -o gtw-agent-linux-arm64 ./cmd/gtw-agent

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            gtw-agent-linux-amd64
            gtw-agent-linux-arm64
          generate_release_notes: true